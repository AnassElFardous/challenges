#include <stdio.h>
#include <stdlib.h>

int main()
{
    int nombre = 5;

  if (nombre % 2 == 0) {
    printf("%d is pair\n", nombre);
  } else {
    printf("%d is impair\n", nombre);
  }

return 0;
}
